import sys

num_steps = int(sys.argv[1])

now_steps = 0

gap_steps = num_steps

while now_steps < num_steps:
    now_steps = now_steps + 1
    gap_steps = gap_steps - 1
    
    g = " " * gap_steps
    s = "#" * now_steps
    
    sum = str( g + s)
    
    print(sum)
